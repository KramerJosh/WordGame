import LetterRow from "./LetterRow";

const testWord = "STINK"

//LetterRow needs to take in either a bunch of props, or one prop formatted as an array

const WordArea = () => {

    return (
        <LetterRow />
    )
}

export default WordArea